import {articles} from './articles';
export default function Home(){const [feature,...rest]=articles;return <main><div className="announcement">Independent advice for happier pets · New guides every week</div><nav><a className="brand" href="#top">🐾 BarkyTalk</a><div className="links"><a href="#stories">Latest stories</a><a href="#guides">Guides</a><a href="#promise">Our promise</a></div><a className="pill" href="#newsletter">Join the pack</a></nav><section className="editorial-hero" id="top"><div className="feature-copy"><p className="eyebrow">FEATURED STORY · {feature.readTime}</p><h1>{feature.title}</h1><p className="lede">{feature.excerpt}</p><a className="button" href={`/article/${feature.slug}`}>Read the full story →</a></div><a className="feature-image" href={`/article/${feature.slug}`}><img src={feature.image} alt={feature.imageAlt}/><span>Photo by {feature.credit}</span></a></section><section className="section" id="stories"><header className="section-head"><div><p className="eyebrow">FRESH FROM THE FIELD NOTES</p><h2>Useful reads for<br/><em>real pet life.</em></h2></div><p>Carefully written guides with practical steps, thoughtful product criteria, and no filler.</p></header><div className="story-grid">{rest.map(a=><article className="story-card" key={a.slug}><a href={`/article/${a.slug}`}><img src={a.image} alt={a.imageAlt}/></a><p className="eyebrow">{a.category} · {a.readTime}</p><h3><a href={`/article/${a.slug}`}>{a.title}</a></h3><p>{a.excerpt}</p><a className="read-link" href={`/article/${a.slug}`}>Read article →</a></article>)}</div></section><section className="guides" id="guides"><div><p className="eyebrow">START HERE</p><h2>Better choices.<br/><em>Happier companions.</em></h2><p>Every BarkyTalk guide is written to help you make one clear, confident next move.</p></div><div>{articles.map((a,i)=><a className="guide" href={`/article/${a.slug}`} key={a.slug}><span>0{i+1}</span><strong>{a.title}</strong><small>{a.readTime}</small><b>→</b></a>)}</div></section><section className="newsletter" id="newsletter"><div><p className="eyebrow">A LITTLE NOTE FOR YOUR INBOX</p><h2>Good advice.<br/><em>No barking.</em></h2></div><form><label className="sr-only" htmlFor="email">Email address</label><input id="email" type="email" placeholder="you@example.com" required/><button>Join the pack →</button><small>One thoughtful email a week. Unsubscribe anytime.</small></form></section><footer id="promise"><a className="brand" href="#top">🐾 BarkyTalk</a><p>BarkyTalk is reader-supported. When you buy through our links, we may earn a commission at no extra cost to you. Recommendations remain independent. General information is not a substitute for veterinary care.</p><span>© 2026 BarkyTalk</span></footer></main>}
