import type {Metadata} from 'next';import {notFound} from 'next/navigation';import {articles,getArticle} from '../../articles';
export function generateStaticParams(){return articles.map(({slug})=>({slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const a=getArticle(slug);if(!a)return{};return{title:`${a.title} | BarkyTalk`,description:a.excerpt,openGraph:{title:a.title,description:a.excerpt,images:[a.image]},twitter:{card:'summary_large_image',title:a.title,description:a.excerpt,images:[a.image]}}}
export default async function ArticlePage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const a=getArticle(slug);if(!a)notFound();return <main><div className="announcement">Independent advice for happier pets</div><nav><a className="brand" href="/">🐾 BarkyTalk</a><div className="links"><a href="/#stories">Latest stories</a><a href="/#guides">Guides</a><a href="/#promise">Our promise</a></div><a className="pill" href="/#newsletter">Join the pack</a></nav><article className="article-page"><header><p className="eyebrow">{a.category} · {a.readTime}</p><h1>{a.title}</h1><p className="article-deck">{a.excerpt}</p><div className="byline"><b>By BarkyTalk Editors</b><span>{a.date}</span></div></header><figure><img src={a.image} alt={a.imageAlt}/><figcaption>Photo by <a href={a.creditUrl}>{a.credit}</a> on Unsplash</figcaption></figure><div className="article-body"><p className="intro">{a.intro}</p>{a.sections.map(s=><section key={s.heading}><h2>{s.heading}</h2>{s.paragraphs.map(p=><p key={p}>{p}</p>)}{s.bullets&&<ul>{s.bullets.map(b=><li key={b}>{b}</li>)}</ul>}</section>)}<aside><b>A note from BarkyTalk</b><p>Products mentioned in our guides may include affiliate links. We evaluate usefulness, materials, safety, and value independently; commissions never determine our conclusions.</p></aside></div></article><footer><a className="brand" href="/">🐾 BarkyTalk</a><p>Practical care guides and genuinely useful finds for life with pets.</p><span>© 2026 BarkyTalk</span></footer></main>}
